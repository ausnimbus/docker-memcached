#!/bin/bash
source cgroup-limits

export MEMCACHED_MAXMEMORY=${MEMCACHED_MAXMEMORY:-"$((MEMORY_LIMIT_IN_BYTES/1000000-32))M"}
export MEMCACHED_EXTRA_OPTIONS=${MEMCACHED_EXTRA_OPTIONS:-""}

export OPTIONS=${OPTIONS:-" -m $MEMCACHED_MAXMEMORY $MEMCACHED_EXTRA_OPTIONS"}
